[project]
name = "mp2ne"
version = "0.1.0"
description = "MoviePilot API to NextEmby adapter"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "httpx>=0.27.0",
  "pydantic>=2.8.0",
  "uvicorn[standard]>=0.30.0",
]
