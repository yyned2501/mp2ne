# mp2ne 项目开发规范

这是本项目的总则，AI 在进行任何代码修改或新功能开发时必须严格遵守。

## 1. 核心目标
- 本项目的职责是将 MoviePilot 风格接口适配到 NextEmby 或同类后端服务。
- 所有对外接口必须保持稳定的 MoviePilot 兼容性，避免破坏现有客户端。

## 2. 架构约束
- 统一使用 FastAPI + httpx 实现异步代理。
- 鉴权、路由适配、后端转发必须分层，不得混在一个函数里。
- 公共请求参数、响应转换逻辑应独立封装，避免在路由中直接堆砌。

## 3. 配置规范
- 后端地址、API Key、MoviePilot 模拟 Token 必须通过环境变量或配置文件提供，禁止硬编码真实凭据。
- 示例配置必须与默认行为保持一致。

## 4. 日志与调试
- 所有日志输出统一使用 `logging`，禁止使用 `print`。
- 代理层必须记录请求方法、路径、关键参数、后端响应状态码，便于排障。
- 对于可恢复的网络错误，应返回明确的 502/503，并保留足够上下文日志。

## 5. 代码风格
- 使用类型提示。
- 中文注释用于解释关键适配逻辑。
- 尽量保持单文件职责清晰，避免文件过长。

## 6. 接口对齐约定
- 对外 API 必须尽量对齐 MoviePilot 的返回值结构、字段名和错误风格。
- 数据访问统一面向 NextFind / NextEmby 后端，不在前端路由中硬编码业务数据。
- 列表类接口暂时使用 `Cookie: nextmedia_session=authenticated` 访问后端查询接口。
- 添加订阅与删除订阅必须走 openapi 路径，保持与后端文档一致。
- 写操作必须使用 `X-API-Key`，不得把 cookie 作为写接口主认证方式。
- 路由层只做参数整理、后端转发和 MP 风格结果包装，不得把后端细节暴露给客户端。
